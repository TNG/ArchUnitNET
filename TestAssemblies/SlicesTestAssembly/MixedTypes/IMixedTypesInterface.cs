namespace SlicesTestAssembly.MixedTypes;

// The only interface in this assembly. It lives in a namespace that no slice pattern in the
// tests matches, so it stays out of every slice-assignment and diagram fixture and exists
// purely so Slice's Classes/Interfaces filters can be exercised on a mixed type list.
public interface IMixedTypesInterface { }
