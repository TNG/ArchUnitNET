language: csharp
dotnet: 2.1.0
solution: ArchUnit.sln
install:
  - nuget restore ArchUnit.sln
  - nuget install xunit.runners -Version 1.9.2 -OutputDirectory testrunner
script:
  - dotnet restore
  - dotnet build
  - dotnet test
  - dotnet publish